# Optional: point to the FedVLR training project root
FEDVLR_ROOT=../FedVLR

# Optional: override the results directory directly
# FEDVLR_RESULTS_DIR=../FedVLR/outputs/results

# Optional: override the showcase artifacts directory directly
# SHOWCASE_ARTIFACT_ROOT=../FedVLR/outputs/showcase_artifacts
